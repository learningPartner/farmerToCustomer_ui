import { HideForFarmer } from './hide-for-farmer';

describe('HideForFarmer', () => {
  it('should create an instance', () => {
    const directive = new HideForFarmer();
    expect(directive).toBeTruthy();
  });
});
